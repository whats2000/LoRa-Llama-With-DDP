# ── exp07 / e07b — few_shot r=256 @ eff batch 192 (stronger ORTHOGONAL member) ──
paths:
  saved_models: "experiments/exp07_eff192_ensemble/e07b/saved_models"
  output_csv: "experiments/exp07_eff192_ensemble/e07b/outputs/few_shot_submission.csv"
lora: {r: 256, lora_alpha: 512}
training: {learning_rate: 5e-5, batch_size: 24, grad_accumulation_steps: 1}
prompting: {strategy: "few_shot", num_few_shot_examples: 4}
