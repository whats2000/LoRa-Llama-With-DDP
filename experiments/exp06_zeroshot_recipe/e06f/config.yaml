# ── exp06 / e06f — zero_shot r=256, eff384 2x-steps (steps lever; lr 5e-5) ──
paths:
  saved_models: "experiments/exp06_zeroshot_recipe/e06f/saved_models"
  output_csv: "experiments/exp06_zeroshot_recipe/e06f/outputs/zero_shot_submission.csv"
lora: {r: 256, lora_alpha: 512}
training: {learning_rate: 5e-5, batch_size: 48, grad_accumulation_steps: 1}
prompting: {strategy: "zero_shot"}
