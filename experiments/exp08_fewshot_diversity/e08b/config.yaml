# ── exp08 / e08b — few_shot 2-shot @ eff192 (fewer in-context = different view) ──
paths:
  saved_models: "experiments/exp08_fewshot_diversity/e08b/saved_models"
  output_csv: "experiments/exp08_fewshot_diversity/e08b/outputs/few_shot_submission.csv"
lora: {r: 256, lora_alpha: 512}
training: {learning_rate: 5e-5, batch_size: 24, grad_accumulation_steps: 1}
prompting: {strategy: "few_shot", num_few_shot_examples: 2}
